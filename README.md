# Aveith (Instagram-lite)

Fullstack social media app (MERN) with posts, follows, and stories.

## Local dev

1. Install deps
   - `npm install`

2. Configure env
   - `backend/.env` (see `backend/.env.example`)
   - `frontend/.env` (see `frontend/.env.example`)

3. Run
   - `npm run dev`

